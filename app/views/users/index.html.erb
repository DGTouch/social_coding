<div class="container">

<% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: name %>
  <% end %>


<ul>
  <% @users.each do |user| %>

  <li> <%= user.name %> <%= link_to "View Profile", user_path(:id => user.id) %> <li>
  <% end %>
</ul>

</div>
